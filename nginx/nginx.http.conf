server {
    listen 80;
    server_name denotenman.com www.denotenman.com api.denotenman.com admin.denotenman.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}
